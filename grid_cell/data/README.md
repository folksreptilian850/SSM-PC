Place the self-navigation maze datasets here, or set the `GRID_DATA_ROOT` environment variable to point to your dataset directory.
Each dataset folder should follow the pattern `D*_P*` and contain numbered trajectory subdirectories with `frame_info.json` and image data.
