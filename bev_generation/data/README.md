Place BEV training/validation datasets here, or set the `BEV_DATA_ROOT` environment variable to override the default location.
Dataset directories should follow the pattern `D*` and contain `fps_views`, `bev_views`, and associated JSON metadata.
